# MSFT Incident Report

GitHub Pages-ready incident reporting form.

Set GitHub Pages to:

- Branch: `main`
- Folder: `/docs`

This package places `docs/index.html` at the top level when extracted. The app is self-contained and does not require React, npm, CDN links or a build step.
